<h1>New Tweet</h1>

<p>
<%= form_for :post, url: posts_path do |f| %>

  <% if @post.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@post.errors.count, "error") %> prohibited this tweet from being saved:
    </h2>
    <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><p style="color:red"> <%= msg %> </li></p>
      <% end %>
    </ul>
  </div>
  <% end %>

<%= f.label :tweet %> <br>
<%= f.text_area :text %> <br>

</p>

<p>
<%= f.submit %>
</p>

<%= link_to "Back", posts_path %>
<% end %>